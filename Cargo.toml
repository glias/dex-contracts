[workspace]
members = [
    "tests",
    "contracts/ckb-dex-contract",
    "contracts/liquidity-poll-contract",
    "share"
]

[profile.release]
overflow-checks = true
opt-level = 's'
lto = true
codegen-units = 1
panic = 'abort'