# [rust]
# # path of rust contracts workspace directory,
# # a `Cargo.toml` file is expected under the directory.
# workspace_dir = "."

# capsule version
version = "0.4.4 fae2bb1"
# path of deployment config file
deployment = "deployment.toml"

[[contracts]]
name = "asset-order-lockscript"
template_type = "Rust"

[[contracts]]
name = "liquidity-poll-contract"
template_type = "Rust"
